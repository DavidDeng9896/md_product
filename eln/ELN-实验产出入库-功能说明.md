# ELN 实验产出 — 入库功能说明

## 版本说明

| 版本 | 修订内容 | 修订时间 |
| --- | --- | --- |
| V1.0 | 初稿：基于明博智云 ELN 运行界面梳理，并对齐《资源管理-需求说明》预入库登记流程与 Registry 关联物料能力 | 2026-09-17 |

**文档范围**：本文仅描述 ELN **系统-实验产出** 模块中的 **入库登记** 能力（含实验记录内列表态、入库抽屉、与物料库/Registry 的衔接）。不包含样品检测、物料领用等其他 ELN 模块。

**术语约定**：

| 术语 | 含义 |
| --- | --- |
| 实验产出 / 实验产物 | 同一业务能力的不同表述；UI 模块名称为 **系统-实验产出**，物料侧称 **实验产物登记** |
| 入库登记 | 实验员在 ELN 中填写产出物信息并提交，生成 **待上架** 的预入库数据（尚未完成正式库存入账） |
| 确认入库 | ELN 侧提交登记的单次操作（不等同于库管「生成入库单」） |

---

## 1. 功能概述

### 1.1 功能定位

实验产出入库是 ELN 实验记录本中的嵌入式模块，用于在实验进行或完成后，**登记本实验产生的物料/产物**，并将其推送至资源管理 **预入库登记（已登记物料）** 队列，由库管后续选择仓库库位、生成入库单，完成正式入库。

该模块解决的核心问题：

- 实验员无需离开实验记录，即可登记产出物基本信息、批次信息与临时存放信息；
- 产出物与 **来源实验** 自动关联，支持后续追溯；
- 与 Registry **注册数据**、对象↔物料映射能力衔接，减少重复录入；
- 与采购到货登记区分：**实验产物登记数据在物料库侧不可修改**，仅允许上架与提交。

### 1.2 访问入口

| 入口 | 说明 |
| --- | --- |
| ELN → 实验记录 → 正文模块 **系统-实验产出** | 主入口；模块作为 Block 嵌入实验记录流 |
| ELN → 实验记录本左侧导航 **系统-实验产出** | 快捷定位到当前实验记录下产出模块 |
| 物料库 → 物料配置 → 物料主数据 | 与「ELN-实验记录-产出入库」并列的主数据维护入口（Registry PRD） |

### 1.3 典型使用场景

1. **化学合成**：反应完成后登记目标化合物，填写批次号、纯度、产量与容器信息；
2. **生物制备**：登记细胞、质粒、蛋白等产出，关联 Registry 注册实体；
3. **提取/制备类实验**：与「提取液制备」等步骤区块并列，登记中间体或终产物；
4. **批量产出**：同一实验一次产生多条产出，使用「批量入库」逐条或合并登记。

---

## 2. 角色与权限

| 角色 | 能力 |
| --- | --- |
| 实验记录创建人 / 协作人 | 在本实验记录下添加、编辑（草稿态）、删除产出登记；提交入库登记 |
| 实验记录只读用户 | 查看模块列表与已登记产出，不可编辑 |
| 库管 | 在 **物料管理 → 入库管理 → 预入库登记** 查看实验产物登记数据，执行上架入库、生成入库单（见第 9 节） |
| 公司管理员 | 配置物料类型字段、编号规则、仓库与审批策略 |

**权限原则**：

- 模块操作权限继承 **实验记录** 创建人/协作人权限；
- 登记数据的 **公司范围** 与实验记录所属公司一致；
- 仓库选择受 **仓库授权** 与 **锁库状态** 约束（盘点锁库时不可选择该仓库）。

---

## 3. 模块结构（实验记录内）

### 3.1 嵌入形态

「系统-实验产出」以可折叠 Block 形式嵌入实验记录正文，具备：

- **模块标题区**：模块名、展开/折叠、模块级操作按钮；
- **数据列表区**：展示当前实验下已登记的产出记录；
- **空状态**：无数据时显示「暂无数据」。

模块不跳转新页面；登记时在当前实验记录页 **右侧滑出抽屉**（Drawer），标题为 **入库**。

### 3.2 模块级操作

| 操作 | 说明 | 前提 |
| --- | --- | --- |
| **添加** | 打开入库抽屉，新增一条产出登记 | 实验记录可编辑 |
| **批量入库** | 对多条待登记/草稿产出批量进入入库流程 | 列表存在可批量处理的记录 |
| **删除** | 删除选中产出登记 | 仅草稿或未提交态；已推送预入库且被库管处理的记录不可删 |
| **打印** | 打印产出标签或登记信息（若配置） | 可选能力 |
| **更多操作** | 模块扩展菜单 | — |

### 3.3 列表展示（有数据时）

列表展示当前实验关联的产出登记摘要，建议列：

| 列名 | 说明 |
| --- | --- |
| 物料编号 | 按物料类型编号规则生成或手工指定 |
| 物料名称 | 主数据名称 |
| 物料类型 | 业务类型名称 |
| 批次号 | 批次标识 |
| 入库总量 | 容器数量合计 |
| 登记人 | 提交登记的用户 |
| 登记时间 | 提交时间 |
| 状态 | 草稿 / 已登记（待上架）/ 已入库 |
| 操作 | 编辑、删除、查看 |

> 具体列集可按物料类型配置扩展字段摘要；空列表时显示「暂无数据」。

### 3.4 负责人（可选）

模块可要求指定 **负责人/登记人**（从记录本成员或项目成员中选择），未指定时不允许提交。人员选择弹窗：

- 标签页：**记录本成员** / **项目成员**
- 列表列：账号、姓名、部门
- 支持搜索；底部展示已选人员；**确定** / **取消**

---

## 4. 入库抽屉 — 界面结构

### 4.1 抽屉概览

| 项目 | 说明 |
| --- | --- |
| 标题 | **入库** |
| 触发 | 点击「添加」、列表行「编辑」或「批量入库」 |
| 布局 | 右侧半屏 Drawer，不离开当前实验记录 |
| 分区 | 基本信息 → 扩展信息（动态）→ 批次信息 → 存放信息 |
| 分区交互 | 各分区支持展开/折叠 |

抽屉底部操作：

| 按钮 | 类型 | 行为 |
| --- | --- | --- |
| **确认入库** / **保存并入库** | 主按钮 | 校验通过后提交登记，关闭抽屉，数据进入预入库队列 |
| **保存** | 次按钮 | 保存为草稿，不推送预入库 |
| **取消** | 次按钮 | 关闭抽屉；有未保存修改时二次确认 |

> 不同环境按钮文案可能为「确认入库」或「保存并入库」，产品统一为：**主按钮 = 提交登记**。

---

## 5. 表单字段说明

字段分为 **固定字段** 与 **按物料类型动态渲染字段**。动态字段规则与《资源管理-需求说明》预入库登记编辑窗一致：由物料类型的 **基础信息、扩展信息、批次信息** 配置决定。

### 5.1 基本信息

| 字段 | 必填 | 控件 | 说明 |
| --- | --- | --- | --- |
| **生成方式** | 视配置 | 下拉 | 产物产生方式（如合成、提取、发酵等）；部分物料类型显示 |
| **物料类型** / **物料分类** | 是 | 下拉 | 选择产出所属物料类型（须 **已发布 + 启用**）；切换后刷新下方动态字段 |
| **审批提示** | — | 只读标签 | 如「不需要审批」，表示该类型登记无需走审批 |
| **物料编号** | 否 | 文本 | 按物料类型编号规则 **自动生成**；可提前手工指定（若规则允许） |
| **物料名称** | 是 | 文本 | 默认提示：「根据物料分类和生成规则自动生成」；可手工修改 |
| **计量单位** | 是 | 下拉 | 随物料类型默认带出，可改 |
| **保存物料主数据** | 否 | 复选 | 勾选时同步创建或更新 **物料主数据**；不勾选则仅登记批次/库存预入库数据 |
| **注册数据** | 视类型 | 对象选择器 | 物料类型启用【注册数据】时显示；从 Registry 选择注册实体，名称同步为「编号 (名称)」 |
| **化合物** | 视类型 | 结构编辑器 | 普通物料且启用【化合物】时显示；与【注册数据】互斥 |

**普通物料常见扩展基础字段**（物料类型配置为显示时出现）：

规格型号、别名、浓度、纯度、含量、生产厂家、级别、外文名称、图片等。

**生物类物料（细胞/质粒/菌种/病毒/抗体/引物）** 基础信息字段见《物料类型默认属性.md》；6 种生物类基础信息字段一致（7 个）。

### 5.2 扩展信息

按物料类型 **扩展字段配置** 动态渲染（单行文本、多行文本、计数器、下拉、开关、图片、上传等 11 种类型）。

规则：

- 必填性、只读、校验条件（长度、正则、数值区间）遵循物料类型扩展字段 PRD；
- 图片/上传类字段支持在 ELN 登记时上传附件；
- **对象↔物料映射** 启用时，对象侧已填字段可按映射规则 **预填** 至物料扩展字段（见第 10 节）。

### 5.3 批次信息

| 字段 | 必填 | 控件 | 说明 |
| --- | --- | --- | --- |
| **批次号** | 是 | 文本 | 产出批次标识；可与实验批号策略联动 |
| **备注** | 否 | 多行文本 | 最多 500 字，显示字符计数 |
| **来源实验** | 系统 | 只读/隐式 | **自动写入当前实验记录**，不在表单展示或不可编辑 |
| **有效期至** | 视类型 | 日期 | 批次字段，按物料类型配置 |
| **所属项目** | 视类型 | 下拉/文本 | 默认继承实验记录项目 |
| **检测状态** | 视类型 | 下拉 | 生物类批次字段 |
| **批号、生产日期、COA、父细胞、关联序列等** | 视类型 | 各异 | 见物料类型批次字段配置 |

> **映射规则**：对象↔物料映射 **不包含批次字段**；批次相关内容均在入库抽屉录入，不从 Registry 对象映射带入。

### 5.4 存放信息

#### 5.4.1 存放位置

| 字段 | 必填 | 控件 | 说明 |
| --- | --- | --- | --- |
| **容器** | 是 | 下拉 | 容器类型（瓶、管、孔板等），决定默认容器编号规则 |
| **仓库** / **存放位置** | 否 | 级联下拉 | 实验员登记阶段可填 **临时存放位置**（库房 → 仓库 → 货区/库位）；也可留空，由库管上架时指定 |
| **库位** | 视仓库 | 库位选择 | 仓库开启库位管理时，在容器明细行中选择 |

**约束**：

- 仓库处于 **盘点锁库** 状态时，不可选择；
- 仓库 **可管理物料类型** 须包含当前物料类型；
- 孔板库位须选到具体孔位（如 `库位1\1\-2\A1`）。

#### 5.4.2 容器信息明细表

通过 **「+新增」** 添加容器行。

| 列名 | 说明 |
| --- | --- |
| 序号 | 行号 |
| 容器编号 / 管理编号 | 按容器类型规则自动生成，或手工指定 |
| 数量 | 入库数量，必填 |
| 单位 | 默认继承计量单位 |
| 开瓶日期 | 容器配置字段（若启用） |
| 库位 | 仓库开启库位管理时显示；支持双击编辑、批量修改库位 |
| 操作 | 删除行 |

表格空态显示「暂无数据」。**入库总量** = 各容器数量之和，在表单底部汇总展示。

---

## 6. 业务规则

### 6.1 编号与命名

| 规则 | 说明 |
| --- | --- |
| 物料编号 | 按物料类型 **编号规则** 生成；实验产出登记时可编辑（若类型允许） |
| 物料名称 | 可自动生成；勾选「保存物料主数据」且选择注册数据时，名称格式为「编号 (名称)」 |
| 容器编号 | 按容器类型规则生成；整体出库/整体使用策略在后续领用环节生效 |
| 批次号 | 用户填写；可与实验内部批号一致，但不要求与物料主数据编号相同 |

### 6.2 主数据与唯一性

| 场景 | 行为 |
| --- | --- |
| 勾选「保存物料主数据」且为新物料 | 创建物料主数据 + 预入库登记 |
| 未勾选 | 仅创建预入库登记（须能匹配已有主数据或允许无主数据登记，按类型策略） |
| 主数据唯一性命中 | 引用已有主数据，**不新建**；Registry 映射场景以 **注册数据** 为锚点 |
| 实验产物登记后在物料库 | **任何人都不可修改物料信息**，仅可上架与提交 |

### 6.3 保存 vs 确认入库

| 操作 | 数据态 | 下游可见性 |
| --- | --- | --- |
| **保存** | 草稿 | 仅 ELN 模块内可见，预入库登记不可见 |
| **确认入库** | 已登记（待上架） | 推送至 **预入库登记 / 已登记物料** 列表 |

### 6.4 直接上架（可选能力）

若实验员在登记时已选择 **仓库 + 库位 + 容器明细完整**，且策略允许，可支持 **直接生成入库单**（对齐资源管理「直接上架」）。默认实验产物流程为 **两阶段**：实验员登记 → 库管上架（见第 9 节）。

---

## 7. 校验规则

### 7.1 提交前校验（确认入库）

1. **必填字段**：物料类型、物料名称、计量单位、批次号、容器、容器明细至少一行且数量 > 0；
2. **负责人**：若模块要求指定负责人，须已选择；
3. **物料类型有效性**：类型须已发布、已启用、与实验记录同公司；
4. **容器有效性**：容器类型属于当前物料类型允许范围；
5. **仓库**（若填写）：仓库有效、未锁库、可管理当前物料类型、用户有入库业务权限；
6. **库位**（若填写）：库位有效、未被占用（孔板按孔位校验）；
7. **扩展字段**：满足物料类型配置的校验条件与必填项；
8. **注册数据**（若填写）：注册实体有效、用户有查看权限、与物料类型注册对象匹配。

### 7.2 校验失败交互

- 字段级：必填项标红，聚焦首个错误字段；
- 表单级：Toast 提示原因（如「请选择负责人」「当前仓库已锁库，无法使用」）；
- 不关闭抽屉，保留已填内容。

### 7.3 删除校验

- 草稿：可直接删除；
- 已登记且库管未处理：允许撤回（从预入库登记移除），需二次确认；
- 已生成入库单或已入库：不可删除，仅可通过退库/报废等下游流程处理。

---

## 8. 数据状态与流转

### 8.1 状态机

```text
[草稿] ──保存──► [草稿]
   │
   └──确认入库──► [已登记/待上架] ──库管上架──► [已入库]
                        │
                        └──撤回──► [草稿] 或删除
```

| 状态 | ELN 模块 | 预入库登记 | 库存 |
| --- | --- | --- | --- |
| 草稿 | 可编辑、可删 | 不可见 | 无 |
| 已登记（待上架） | 只读；可撤回 | 可见；不可改物料信息 | 无 |
| 已入库 | 只读；展示入库单链接 | 已清除 | 有 |

### 8.2 审计字段

每条产出登记记录：

- 登记人、登记时间；
- 来源实验（实验记录编号，可跳转预览）；
- 所属项目（继承实验记录）；
- 修改人、修改时间（草稿编辑时）。

---

## 9. 与资源管理衔接

实验产出入库登记完成后，数据进入 **物料管理 → 入库管理 → 预入库登记**（已登记物料列表）。

### 9.1 库管侧流程

```text
实验员 ELN 确认入库
    ↓
预入库登记列表（按公司可见）
    ↓
库管选择记录 → 上架入库（指定/确认仓库、库位）
    ↓
提交 → 按仓库拆分生成入库单
    ↓
入库单审批（若仓库开启）→ 已完成 → 库存增加
```

### 9.2 预入库登记侧规则（实验产物特有）

| 规则 | 说明 |
| --- | --- |
| 不可修改物料信息 | 实验产物登记的记录，物料库侧 **任何人都无法修改** 基础/扩展/批次主数据 |
| 允许操作 | 查看、选择仓库库位、上架、提交生成入库单 |
| 列表字段 | 物料编号、名称、类型、容器数量、仓库、入库总量、批次、有效期至、备注、登记人/时间 |
| 批量提交 | 相同仓库合并为一个入库单，不同仓库拆分 |

### 9.3 入库单与入库明细

- 入库单：**物料管理 → 入库管理 → 入库单**；
- 入库明细台账展示入库流水，业务类型含 **验收入库** 等；
- 入库明细/批次信息含 **来源实验** 字段，可跳转 ELN 实验预览。

---

## 10. 与 Registry 衔接

### 10.1 注册数据

当物料类型启用 **【注册数据】** 字段：

- 入库抽屉基本信息区展示注册数据选择器；
- 支持按名称、编号检索 Registry 实体；化合物类型支持结构检索；
- 选中后物料名称同步为「编号 (名称)」；
- 入库完成后，Registry 实体详情 **关联物料** Tab 展示对应库存（容器号、批次、剩余量等）；
- 支持点击 **来源实验** 跳转 ELN 实验预览。

### 10.2 对象 ↔ 物料映射（入库同步）

若当前 Registry 对象配置了 **对象与物料信息映射**（物料类型如「实验产物（普通物料）」）：

| 时机 | 行为 |
| --- | --- |
| 入库登记打开时 | 按映射将对象实体 **基础信息 + 扩展信息** 预填至物料字段 |
| 映射范围 | **不含批次字段**（批号、有效期、来源实验、COA 等在抽屉录入） |
| 主数据唯一性 | 命中则引用已有主数据，**注册数据** 指向源对象实体 |
| 新建对象 | 若策略要求同步生成 Registry 对象实体，且目标对象开启工作流，则进入 **待审批** 态 |

> 运行时入口以 **物料入库/产出入库** 为准，不从 Registry 列表做「复制到物料」主入口。

---

## 11. 批量入库

| 项目 | 说明 |
| --- | --- |
| 入口 | 模块工具栏「批量入库」 |
| 适用 | 同一实验下多条产出待登记，或列表多选 |
| 交互 | 逐条打开入库抽屉，或批量编辑窗（显示「当前 X / 总计 Y」，支持上一个/下一个） |
| 提交 | 每条独立校验；部分失败不影响已成功条目 |
| 限制 | 不同物料类型可混选，但每条须独立完成必填项 |

---

## 12. 边界与异常

| 场景 | 处理 |
| --- | --- |
| 实验记录锁定/归档 | 模块只读，不可新增或编辑 |
| 仓库盘点锁库 | 登记时不可选该仓库；已选则提交拦截 |
| 物料类型被禁用 | 草稿不可提交；已登记数据按存量策略处理 |
| 无仓库权限 | 允许不选仓库登记（临时存放），库管上架时指定 |
| 注册实体无权限 | 注册数据选择器不可见或选择后提交拦截 |
| 化合物与注册数据同时启用 | Toast：「当前【化合物】和【注册数据】只能显示其中一个」 |
| 网络/保存失败 | 保留表单内容，提示重试 |

---

## 13. 非功能需求

| 项 | 要求 |
| --- | --- |
| 性能 | 抽屉打开 ≤ 1s；动态字段渲染 ≤ 2s |
| 兼容 | 与预入库登记编辑窗字段一致，避免 ELN 与物料库两套表单 |
| 追溯 | 来源实验、登记人、登记时间不可篡改 |
| 审计 | 提交、撤回、删除留操作日志 |

---

## 14. 与现有文档关系

| 文档 | 关系 |
| --- | --- |
| [资源管理-需求说明.md](../资源管理/资源管理-需求说明.md) | 实验产物登记入库业务流程、预入库登记、入库单 — **下游流程以该文档为准** |
| [Registry-科学数据注册.md](../registry/Registry-科学数据注册.md) | 关联物料、注册数据、ELN 产出入库入口 |
| [对象复制工作流与物料映射-功能设计说明.md](../registry/对象复制工作流与物料映射-功能设计说明.md) | 对象↔物料映射范围与入库同步规则 |
| [物料类型默认属性.md](../资源管理/物料类型默认属性.md) | 各业务类型字段清单 |
| [ELN-样品业务逻辑梳理.md](./ELN-样品业务逻辑梳理.md) | 样品检测与实验产出并列，业务独立 |

---

## 15. 待定项

| 编号 | 内容 | 备注 |
| --- | --- | --- |
| TBD-01 | 「生成方式」枚举值与物料类型关系 | 需产品确认是否全局字典或按类型配置 |
| TBD-02 | 确认入库 vs 直接上架 默认策略 | 默认两阶段；是否开放实验员直上架 |
| TBD-03 | 草稿保留时长与自动清理 | — |
| TBD-04 | 产出标签打印模板 | 是否复用物料条码模板 |
| TBD-05 | 与合成反应/提取液制备模块的自动带入 | 是否从步骤区块自动预填产出字段 |

---

## 附录 A：入库抽屉字段速查（最小集）

以下为运行界面观测到的 **最小字段集**（未选物料类型时）；完整字段以物料类型配置为准。

**基本信息**：生成方式、物料类型、物料编号、物料名称、计量单位、保存物料主数据

**批次信息**：批次号、备注

**存放信息**：容器、仓库、容器明细表（序号、容器编号、数量、单位、开瓶日期、库位）

**操作**：确认入库、保存、取消

---

## 附录 B：端到端流程图

```text
┌─────────────────────────────────────────────────────────────┐
│  ELN 实验记录                                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  系统-实验产出                                         │  │
│  │  [添加] [批量入库] [删除]                              │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │ 产出列表（编号/名称/批次/状态/登记人…）          │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│         │ 添加 / 编辑                                        │
│         ▼                                                    │
│  ┌─ 入库 Drawer ─────────────────────────────────────────┐  │
│  │ 基本信息 → 扩展信息 → 批次信息 → 存放信息              │  │
│  │ [保存]  [确认入库]  [取消]                             │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
         │ 确认入库
         ▼
┌─────────────────────────────────────────────────────────────┐
│  资源管理 · 预入库登记（已登记物料）                          │
│  库管：上架 → 选仓库/库位 → 提交 → 生成入库单                 │
└─────────────────────────────────────────────────────────────┘
         │ 入库单完成
         ▼
┌─────────────────────────────────────────────────────────────┐
│  库存台账 / 入库明细（来源实验 = ELN 实验记录）               │
│  Registry 关联物料 Tab（若绑定注册数据）                      │
└─────────────────────────────────────────────────────────────┘
```
