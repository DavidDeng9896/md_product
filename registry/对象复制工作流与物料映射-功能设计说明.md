# 功能设计说明：对象复制、工作流与物料映射

## 版本说明

| 版本号 | 修订内容 | 修复原因 | 修订时间 | 变更人 |
| --- | --- | --- | --- | --- |
| V1.0 | 合并《对象复制与工作流》《对象复制映射扩展》；映射交互对齐原型；对象↔物料运行时入口确定为物料入库同步 | 两份说明分述同一能力，入口与弹窗形态未与原型对齐 | 2026-09-16 | — |
| V1.1 | 明确对象↔物料对照两侧字段目录与类型体系不同 | 原型曾把物料类型名套到对象侧，左右看起来一样 | 2026-09-16 | — |
| V1.2 | 物料映射：对象计算字段不能作为写入目标；作为源可映到物料文本/数值 | 误写到对象↔对象；且旧物料说明把对象计算字段一律排除 | 2026-09-16 | — |

本文档为评审/交付用主文档。原两份 V1.0 说明保留作历史，以本文为准。

映射关系设置交互以 [prototype/index.html](./prototype/index.html) 为准。

物料入库页面尚未产出，第三章只定转换关系与规则，页面交互后补。

### 相对旧文档的变更要点

| 项 | 旧说明 | 本文 |
| --- | --- | --- |
| 主文档 | 两份分述：复制+工作流 / 映射扩展 | 一份覆盖配置、对象复制、入库同步、工作流 |
| 映射弹窗 | 逐行「目标 ← 源」，形态未对齐原型 | 右侧全量目标字段、左侧选源可「不设置」；同一行一对；一对一占用（见原型） |
| 对象↔物料运行时 | Registry「复制到物料…」、物料列表「复制到对象…」 | **物料入库页按映射同步**；不再作为主入口 |
| 工作流入口 | 手动新增 + 对象复制 | 加上入库同步生成的对象实体 |
| 对象删除 vs 映射 | 对象删除约束不受映射影响 | 仍成立，见 1.1 |
| 计算字段 | 物料映射中对象计算字段一律不可映射 | 不能写入对象计算字段；**对象→物料**时计算字段可映到物料文本/数值 |

---

## 需求背景

在《Registry 科学数据注册》上补三类能力，一次配齐：

1. **对象复制（对象→对象）**：源对象已注册实体按映射生成目标对象新实体，解决模型字段不一致时的迁移/汇聚，并记录溯源。
2. **对象 ↔ 物料主数据**：实验产物注册与物料主数据有大量共用字段，两套模型不宜强行统一。在对象上配置映射，**在物料入库时按映射同步**，避免两边各录一遍。
3. **注册数据工作流**：对象可挂已有工作流。手动新增、对象复制、以及物料入库同步生成的对象实体，若目标对象开启工作流，均进「待审批」。

```
配置（对象模型 · 映射关系设置）
        │
        ├─ 对象模型映射 ──────────► Registry「复制到...」──► 目标对象新实体
        │                              │
        └─ 对象与物料信息映射 ──► 物料入库页同步（页面后补）
                                      ├─ 写入/复用物料主数据（基础+扩展）
                                      ├─ 必要时生成对象实体
                                      └─ 批次内容仍在入库页录入，不走映射
```

工作流挂在对象模型上，与映射配置独立；复制/同步落库时若目标对象开启工作流，再进入审批。

---

# 一、映射关系设置（配置态）

两类映射都配在**当前对象**上，物料类型侧不设入口。原型入口：对象模型列表「字段映射配置」（条数 + 编辑）。

## 1.1 映射列表

| **A、概述** |  |
| --- | --- |
| **功能模块 / 入口** | 配置中心-参数设置-数据模型设置-对象模型-映射关系设置 |
| **功能说明** | 同一对象可并存多套映射：对象模型映射（对象↔对象）与对象与物料信息映射（对象↔物料类型）。列表展示类型、映射对象、条数、备注、审计字段。 |
| **权限** | 公司管理员；对象编辑权限 |
| **B、功能点** |  |
| **功能点** | **描述** |
| 列表列 | 映射关系（类型标签）、映射对象、映射数量、备注、创建人/创建时间、修改人/修改时间、操作（设置 / 删除） |
| 映射对象文案 | 对象模型映射：`源对象：XXX → 当前对象`；物料映射：`物料类型：XXX` |
| 数量约束 | **对象模型映射**：多对一汇聚，一个目标可绑多个源，同源同目标至多一套。**物料映射**：一个对象可绑多个物料类型；**一个物料类型全局只能被一个对象绑定**，已被绑定的类型在其他对象上不可选 |
| 同公司 | 源对象 / 物料类型与当前对象必须同公司，跨公司不可选 |
| 空状态 | 尚未配置时提示「配置后可从源实体复制到... / 入库时按映射同步」，提供新增 |
| 新增 | 选映射关系类型；对象模型映射再选源对象；物料映射再选物料类型（须「已发布 + 启用」）。未选类型不可下一步 |
| 删除 | 二次确认。删除不影响已复制/已入库数据，但无法再按该规则复制或入库同步。对象删除约束不受映射影响 |
| 切换类型 | 设置弹窗内切换「对象模型映射 / 对象与物料信息映射」视为换表，未保存对应会丢失，需确认 |

## 1.2 字段对照弹窗（对齐原型）

| **功能点** | **描述** |
| --- | --- |
| 弹窗结构 | 第一行：映射关系下拉、备注。对照表表头与「名称 / 字段类型」同一灰底。<br>对象映射：左=源对象下拉（设计库/参考库），右=当前对象。<br>物料映射：左=当前对象（源字段），右=物料类型下拉（实验产物等）；右侧列出该物料类型全部可映射字段 |
| 对应规则 | **同一行即一对映射**。右侧列出**目标侧**全部可映射字段；左侧为该行选择源字段，可不设置。已占用的源字段不可再选（一对一） |
| 对象模型映射范围 | 右侧列出目标对象全部可写入字段。计算字段（关联查找/公式/汇总）不出现在右侧，目标端自算。下拉字段另配选项值子表（见 1.4）。关联字段按 1.5 |
| 物料映射范围 | **仅物料基础信息 + 扩展信息**。批次字段不出现在对照表。右侧是物料字段（物料类型名）；左侧是当前对象字段（Registry 类型） |
| 物料映射 · 计算字段 | **当前对象的关联查找 / 公式 / 汇总不能作为写入目标**（对照表不出现这些目标行，物料→对象时不写入）。**对象作为源时可以映射到物料的文本或数值**（基础信息文本/长文本/数值，扩展单行文本/计数器）。取源实体当时计算值；非文本/数值的物料行（列表/开关/上传等）中这些源字段禁用 |
| 两侧类型体系 | **不可混用**。对象侧：文本 / 数字 / 日期 / 下拉选择 / 勾选 / 附件 / 图片 / 关联 / 用户 / 部门。物料侧基础信息：文本 / 数值 / 列表 / 附件；扩展 11 种：单行文本 / 多行文本 / 计数器 / 滑块 / 开关按钮 / 时间选择框 / 单选框 / 下拉选择框 / 复选框 / 图片 / 上传。物料对照左列是对象类型、右列是物料类型。同名字段如浓度：左「数字」右「数值」 |
| 系统自动写入（物料） | 不占对照行：物料编号（规则生成，新增可改，不继承源编号）、注册数据（指向源对象实体）、计量单位/计量小数位（按物料类型带出） |
| 不可映射 | 物料侧化合物（老业务排除）；对象计算字段不可作为写入目标（见上，不是源侧一律排除） |
| 方向语义 | 配置在当前对象上。对象复制：左侧源对象 → 右侧当前对象。物料入库：对象→物料时按左侧对象字段（含计算字段）写入右侧物料文本/数值；物料→对象时不写入对象计算字段 |

## 1.3 字段类型兼容

### 对象 → 对象

| 目标字段类型 | 可接受源类型 | 处理方式 |
| --- | --- | --- |
| 文本 | 文本/数字/日期/下拉 | 转字符串 |
| 数字 | 数字 | 直接；文本需可转数值 |
| 日期 | 日期 | 按目标格式转换 |
| 下拉 | 下拉 | 选项值子表 |
| 关联 | 关联 | 见 1.5 |
| 用户/部门 | 用户/部门 | 直接搬 |
| 图片/附件 | 图片/附件 | 文件复制 |
| 关联查找/公式/汇总 | — | 不可映射，目标端自算 |

### 对象 ↔ 物料（基础信息）

| 物料侧类型 | 可接受的对象侧类型 | 处理方式 |
| --- | --- | --- |
| 文本/长文本 | 文本/数字/日期/下拉/关联查找/公式/汇总 | 转字符串 |
| 数值（浓度/纯度/含量等） | 数字；关联查找/公式/汇总（结果须可转数值） | 直接；失败按必填/非必填 |
| 列表（生产厂家等） | 下拉/文本 | 同名选项自动匹配，匹配不上留空 |
| 附件/图片 | 附件/图片 | 文件复制 |

### 对象 ↔ 物料（扩展字段 11 种）

| 物料扩展类型 | 可接受的对象侧类型 | 处理方式 |
| --- | --- | --- |
| 单行文本 | 文本/数字/日期/下拉/关联查找/公式/汇总 | 转字符串 |
| 多行文本 | 文本 | 转字符串（保留换行） |
| 计数器 | 数字；关联查找/公式/汇总（结果须可转数值） | 直接；过最小/最大/小数位 |
| 滑块 | 数字 | 直接；过区间 |
| 开关按钮 | 勾选 | 布尔直搬 |
| 时间选择框 | 日期 | 按目标格式 |
| 单选框/下拉选择框 | 下拉（单选） | 同名匹配，失败留空 |
| 复选框 | 下拉（多选） | 逐值同名；部分匹配只搬匹配项 |
| 图片 | 图片/附件 | 文件复制 |
| 上传 | 附件 | 文件复制 |

通用：复制/同步写入要过扩展字段校验与必填；**只读扩展字段允许系统写入**（只读约束表单手工编辑，不约束初始化）；字典变更后老数据不清空，新数据按最新选项文字匹配。反向（物料→对象）对称构造，但**不写入对象计算字段**。对象计算字段作为源时，仅可写入物料文本/长文本/数值（及扩展单行文本/计数器）。

## 1.4 选项映射

| 场景 | 规则 |
| --- | --- |
| 对象↔对象，下拉字段 | 该行展开「源选项 → 目标选项」子表；未指定的源选项复制时留空（必填则阻断） |
| 对象↔物料，列表/单选/下拉/复选 | **不做子表**。同名自动匹配，匹配不上留空（必填则阻断）。字典来源按当前字典；固定选项按固定项 |

## 1.5 关联字段（仅对象模型映射）

1. 源关联指向对象 X、目标关联指向对象 Y：复制时若已配 X→Y 映射则递归复制被关联实体；未配则该关联留空，不阻断。
2. X=Y：直接沿用源关联值。

物料映射不含批次里的父细胞/关联质粒等关联，不走本节。

---

# 二、对象复制（对象 → 对象）

配置来自「对象模型映射」。源侧发起，目标按自身规则新建实体。

## 2.1 复制到

| **A、概述** |  |
| --- | --- |
| **功能模块 / 入口** | Registry-科学数据注册-对象列表/对象详情 |
| **功能说明** | 按源对象已配映射列出可选目标，确认后复制。支持单条与批量。 |
| **权限** | 源对象查看；目标对象新增 |
| **B、功能点** |  |
| **功能点** | **描述** |
| 单条入口 | 详情「更多」、列表行操作「复制到...」。仅**有效**实体可作源（待审批/审批中/停用不可） |
| 批量 | 列表勾选 →「复制到...」。逐条确认、逐条落库、逐条校验；一条失败不影响其他，结束给成功/失败明细 |
| 目标选择 | 列出与源存在对象模型映射的目标（同公司）；显示名称、类型、图标；可搜索 |
| 确认弹窗 | 形态同手动注册，预填映射结果。**映射数据只读**。基础信息默认继承源（部门/项目/分享人/可见范围/别名），**均可改**。未映射的目标必填高亮，弹窗内补。序列对象复制序列；化合物复制结构 |
| 提交校验 | 必填空则阻断；非必填映射不上或转换失败留空 |
| 落库 | 按目标名称规则生成新编号/名称（不继承源编号）。写入映射数据 + 用户改过的基础信息。自动写「来源关联」指向源实体。目标开启工作流则进待审批（第四章） |
| 复制后 | 每次都新建；重复复制=多条。源后续变更不影响已复制目标，无同步、无「重新同步」 |

## 2.2 来源关联（对象复制溯源）

| **功能点** | **描述** |
| --- | --- |
| 字段 | 目标对象内置系统级「来源关联」（关联对象类型），复制时自动写，并记复制时间、操作人。对象模型设置中无配置入口，用户不可配不可删 |
| 展示 | 实体详情显示来源标签；hover 源实体摘要，点击进源详情 |
| 语义 | 只回溯，不更新、不同步。与工作流不冲突 |

---

# 三、对象 ↔ 物料（物料入库同步）

配置来自「对象与物料信息映射」。**运行时不从对象列表/物料主数据列表做「复制到物料」「复制到对象」**，统一由**物料入库页面**按映射同步。

物料入库页面尚未设计，本节只锁定关系；字段布局、按钮文案、确认步骤后补。

## 3.1 关系与职责

| 项 | 约定 |
| --- | --- |
| 触发 | 用户在物料入库（含预入库/产物入库同类入口）提交时，系统按对象上已配的物料映射做主数据层同步 |
| 映射用什么 | 入库涉及的对象实体 + 物料类型，命中「该对象 ↔ 该物料类型」的那一套对照 |
| 映射写什么 | 仅主数据**基础信息 + 扩展信息** |
| 映射不写什么 | 批次信息（批号、有效期、父细胞、COA、供应商等）仍在入库页填写 |
| 对象→主数据 | 入库挂了注册对象时：按映射从对象填主数据；命中物料类型「主数据唯一性校验」则**不新建、直接引用已有主数据** |
| 主数据→对象 | 入库以物料为主、需要落一条注册对象时：按同一套映射生成对象实体；仅「启用」主数据可作为取值源；目标对象若开启工作流，进待审批 |
| 未配映射 | 不自动同步主数据/对象字段；入库仍可按现有手工填写（具体拦截策略随入库页后补） |
| 前置 | 同公司；物料类型「已发布 + 启用」；作为源的对象须为有效（若从对象取值） |

## 3.2 同步时的系统写入与判重

| **功能点** | **描述** |
| --- | --- |
| 物料编号 | 新建主数据时按规则生成，允许改；不继承对象编号 |
| 注册数据 | 物料类型开启该字段时，写入指向对象实体；编号参与主数据唯一性校验（既有行为）；物料名称同步为「编号 (名称)」（既有行为） |
| 计量 | 单位/小数位按物料类型带出 |
| 主数据状态 | 新建默认「启用」 |
| 只读扩展字段 | 允许系统写入 |
| 判重 | 按物料类型唯一性校验（可多字段组合）。命中则该入库物料**引用已有主数据**，不新建；源对象不受影响 |
| 批次对应 | 批号与源对象编号保持一致等批次规则，走入库链路，不走映射表 |

## 3.3 溯源（注册数据锚点）

不新建「来源关联」。对象↔物料靠物料主数据「注册数据」指向对象实体（物料类型需开启该字段；未开启则只靠业务字段）。

| **功能点** | **描述** |
| --- | --- |
| 物料侧 | 详情查看注册数据时显示对象标签，hover 摘要（既有） |
| 对象侧 | 「关联物料」tab 显示库存（容器号、批次、剩余量等，既有） |
| 语义 | 源对象变更不自动改主数据；主数据变更不反向改对象 |

## 3.4 入库页后补清单

入库页完善时需补：页面结构、从对象带出/回写对象的操作点、映射失败与必填补录交互、与预入库/ELN 产出入库的衔接、无映射时的提示或降级。规则以 3.1–3.3 为准，不另开「复制到物料/复制到对象」主入口。

---

# 四、注册数据工作流

与映射配置独立，挂在对象模型上。

## 4.1 设置

| **功能点** | **描述** |
| --- | --- |
| 开关 | 对象设置「是否开启工作流」，默认关。关闭则按原流程直接「有效」 |
| 挂载 | 开启后从系统已有工作流**单选**一个。Registry 不定义工作流 |
| 走审批的入口 | 1. 手动新增注册 2. 对象复制入库 3. **物料入库同步生成的对象实体**。导入、ELN 实验注册等视为已确认，不进临时态 |

## 4.2 临时编号与状态

临时编号：`前缀 + 年月日 + 3 位流水`（如 ABC-20260914-001），按天重置。审批通过后按对象名称规则赋正式编号，不继承临时编号。临时编号转正后只留操作日志，页面不展示。

待审批/审批中禁止被引用、不参与关联查找/汇总、不能被实验引用。因此子项引用时父项必为有效。

| 状态 | 修改 | 删除 | 可被引用 | 编号 |
| --- | --- | --- | --- | --- |
| 待审批 | ✅ | ✅ | ❌ | 临时 |
| 审批中 | ❌ | ❌ | ❌ | 临时 |
| 有效 | ✅ | ✅ | ✅ | 正式 |
| 停用 | — | — | — | 正式（沿用） |

```
新增 / 对象复制 / 入库同步生成对象（开启工作流）
   │ 临时编号
   ▼
[待审批] ──可改──► 仍待审批
   │ 发起审批                 │ 关工作流：可直接转有效
   ▼                        │ 换工作流：按新流程重提
[审批中] ──驳回──► [待审批] ◄── 撤回
   │ 通过 → 正式编号
   ▼
[有效] ⇄ [停用]
```

## 4.3 审批与可见性

| **功能点** | **描述** |
| --- | --- |
| 可见性 | 待审批/审批中：创建人可见，他人隐藏，管理授权权限人可见。转正后按原可见范围 |
| 提交 | 待审批点「发起审批」→ 审批中，锁定 |
| 驳回/撤回 | 驳回或撤回 → 待审批，可改后重提或删除 |
| 通过 | 赋正式编号，转有效 |
| 转正后编辑 | 不再审批 |
| 批量 | 多条临时数据逐条独立审批 |

## 4.4 工作流变更与各模块

| 场景 | 处理 |
| --- | --- |
| 关闭 | 存量待审批：可走原流程重提，或直接转有效 |
| 更换 | 存量待审批：按新工作流重提 |
| 变更时审批中 | 须完成当前流程或先撤回 |
| 存量有效 | 不受对象后来开启工作流影响 |

| 模块 | 临时/审批中 |
| --- | --- |
| 列表/详情 | 按可见性；按钮按状态矩阵 |
| 关联查找/汇总/公式 | 不纳入计算 |
| 实体关系图 | 仅创建人及授权人可见，他人占位 |
| 引用关系 | 不显示（不可被引用） |
| 对象复制源 | 仅有效可作源 |
| 导入导出 | 导出含临时（按可见性）；导入不走审批 |
| ELN/样品 | 不可引用临时/审批中 |

---

# 五、校验策略汇总

| 场景 | 处理 |
| --- | --- |
| 必填未映射且未补录 | 阻断 |
| 非必填未映射/转换失败 | 留空 |
| 对象下拉选项未配对应 | 留空（必填阻断） |
| 物料选项同名匹配失败 | 留空（必填阻断）；复选部分匹配只搬匹配项 |
| 关联未配 X→Y | 该关联留空，不阻断 |
| 计算字段（物料映射） | 不写入对象计算字段。对象→物料：计算字段可映到文本/数值，取当时计算值；转失败留空（必填阻断） |
| 跨公司 | 禁止配置与执行 |
| 物料类型未发布/禁用 | 禁止入库同步 |
| 命中主数据唯一性 | 不新建，引用已有主数据 |
| 目标对象开启工作流（对象复制或入库生成对象） | 待审批 |
| 化合物（物料老字段） | 不映射 |
| 批次字段 | 不映射，入库页录入 |

---

# 六、与既有功能衔接

| 模块 | 说明 |
| --- | --- |
| 对象模型 | 映射关系设置 + 工作流开关，互不影响 |
| 手动注册 | 确认弹窗可复用于对象复制；开启工作流则同样待审批 |
| 物料主数据手工新增 | 入库同步新建的主数据：编号可改、唯一性、启用/禁用，与手工新增一致 |
| 物料类型版本 | 类型升级后，已有主数据沿用「是否更新到最新」 |
| 关联物料 tab | 经注册数据锚点的物料，在对象详情展示库存 |
| 导入导出 | 同步生成的主数据与导入数据同等导出 |

---

# 七、功能清单

| 主模块 | 次级模块 | 功能名称 | 功能简述 | 优先级 |
| --- | --- | --- | --- | --- |
| 配置中心 | 对象模型 | 映射关系设置 | 两类映射并存；右侧全量目标字段、左侧选源可跳过（见原型） | 高 |
| 配置中心 | 对象模型 | 工作流设置 | 开启并挂载已有工作流 | 高 |
| Registry | 科研数据注册 | 复制到（单条/批量） | 对象→对象，确认弹窗、校验、落库 | 高 |
| Registry | 科研数据注册 | 来源关联 | 对象复制溯源标签 | 高 |
| Registry | 科研数据注册 | 审批流转 | 四态、临时编号/正式编号、可见性与锁定 | 高 |
| Inventory / ELN | 物料入库 | 按映射同步对象与主数据 | 入库时用物料映射写/复用主数据，必要时生成对象；**页面后补** | 高 |
| Inventory | 物料主数据 | 注册数据锚点 | 同步时写入注册数据，两侧互链 | 高 |

---

# 八、遗留事项

| 事项 | 计划 |
| --- | --- |
| 物料入库页交互（布局、操作点、失败与补录、与预入库/ELN 产出衔接） | 入库页设计时补第三章细节，不改映射规则 |
| 对象复制确认弹窗的视觉稿 | 规则已定（映射只读、基础信息可改），出视觉即可 |
| 对象模型映射的选项值子表交互 | 配置态细则，原型可后补 |
| 扩展字段校验规则集（正则/区间/步长） | 实现前与《物料类型扩展字段配置 PRD》对齐 |
| 图片/上传大小与数量上限 | 按存储策略 |

---

# 九、文档关系

| 文档 | 角色 |
| --- | --- |
| 本文 | 评审/交付主文档 |
| [对象复制与工作流-功能设计说明.md](./对象复制与工作流-功能设计说明.md) | 历史 V1.0 |
| [对象复制映射扩展-功能设计说明.md](./对象复制映射扩展-功能设计说明.md) | 历史 V1.0；其中「复制到物料/复制到对象」主入口已被第三章「物料入库同步」替代 |
| [discussions/](./discussions/) | 决策过程 |
| [prototype/index.html](./prototype/index.html) | 映射关系设置原型 |
