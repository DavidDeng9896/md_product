
# 2026-09-16

## 原型恢复与界面迭代
- 9-15 晚误将 prototype/index.html 整个重写成简化版覆盖了打磨版；今日依据 9-15 记忆日志中的打磨版细节记录完成重建（Modal1 卡片表格+彩色类型标签、Modal2 红星必填+下一步联动校验、Modal3 返回按钮+行内编辑保存/取消、系统写入行蓝色提示）
- 重建版将两套映射表格（对象模型映射 6 行 / 物料映射 13+4 系统写入行）静态写入 HTML，切换类型仅显示/隐藏，杜绝此前 innerHTML 动态渲染导致的空白问题；行内编辑用 data-mapped/data-value 状态驱动
- 静态校验脚本（node）：JS 语法 / 重复 ID / getElementById 缺失引用 / tbody 行数，全部通过
- 浏览器实测受阻：agent-browser 安装成功但 Chromium 下载超时（GCS 网络问题）；系统 ms-playwright 有 chromium-1234，但 bash 下 agent-browser open 反复 SIGTERM（沙箱限制），自动化验证未跑通
- 用户 11:10 提供真实系统截图：蓝色顶栏 + 白色双列侧栏 + 对象模型完整字段表格（含所属公司/适用范围/审计字段，字段映射配置列=数字+铅笔图标，操作=编辑/停用/删除）
- 按截图两轮修正最外层后，用户最终要求：去掉 header 和左右导航，只保留内容区（已执行，弹窗完整保留）
- 教训（再次确认）：改用户已有文件前必须先确认范围，禁止整文件重写；大改前先备份

## 映射设置弹窗布局贴近真实系统
- 用户提供真实系统「映射关系设置」截图：映射关系/备注双列表单 → 设计库下拉 vs 注册库(当前对象模型) 标题 → 左右字段对照表（拖拽手柄 + 名称 + 字段类型）
- 对照后主要差距：原弹窗把左右表做成两张独立圆角卡片、控件偏高、表与源选择未同一套分栏
- 已调整：Ant Design 32px 控件、同一 2 列网格对齐、去掉表卡片边框、灰表头+行分割线、弹窗宽度收至 920px
- 对照截图二次修正：左右字段表改为无间隙 `map-split`（中间竖线），表头 `#f5f5f5` 连成一条灰带，避免两块独立圆角表头
- 三次修正：设计库下拉 / 注册库标题回到对照表上方；表头恢复「名称 | 字段类型」，与真实系统截图一致（左侧名称格保留源字段下拉以便选择匹配）

## 表头与匹配方式（用户确认）
- 左右表头直接用弹窗第二行：左=源对象下拉（设计库），右=注册库（当前对象模型）；去掉重复的「名称 / 字段类型」表头
- 右侧列出当前对象全部目标字段；左侧按行下拉选择源字段，不需要的选「不设置」跳过
- 已占用的源字段在其他行禁用，避免一对多
- 设计库下拉去掉输入框描边，与「名称 / 字段类型」收进同一条灰表头（注册库标题同样）
- 下拉改为浮层菜单（避免弹窗 overflow 裁切）；左右合并为一张表，固定行高，保证同行对齐

## 待办
- 弹窗内页与真实系统的贴近度待用户再确认（用户已表示原型就这样）

## 功能设计说明合并（用户确认原型后）
- 将 `对象复制与工作流-功能设计说明.md` 与 `对象复制映射扩展-功能设计说明.md` 合并为新主文档：`registry/对象复制工作流与物料映射-功能设计说明.md`
- 对象↔物料转换关系确定从**物料入库页面**同步过来；该页面尚未产出，第三章只锁规则，交互后补
- 不再以 Registry「复制到物料…」/ 物料列表「复制到对象…」作为主入口；对象→对象仍走「复制到...」
- 工作流覆盖：手动新增、对象复制、入库同步生成的对象
- 原两份 V1.0 保留作历史，文首指向新主文档
- 映射交互以当前原型为准：右侧全量目标字段、左侧选源可「不设置」、同一行一对、一对一占用
