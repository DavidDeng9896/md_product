
# 2026-09-16

## 原型恢复与界面迭代
- 9-15 晚误将 prototype/index.html 整个重写成简化版覆盖了打磨版；今日依据 9-15 记忆日志中的打磨版细节记录完成重建（Modal1 卡片表格+彩色类型标签、Modal2 红星必填+下一步联动校验、Modal3 返回按钮+行内编辑保存/取消、系统写入行蓝色提示）
- 重建版将两套映射表格（对象模型映射 6 行 / 物料映射 13+4 系统写入行）静态写入 HTML，切换类型仅显示/隐藏，杜绝此前 innerHTML 动态渲染导致的空白问题；行内编辑用 data-mapped/data-value 状态驱动
- 静态校验脚本（node）：JS 语法 / 重复 ID / getElementById 缺失引用 / tbody 行数，全部通过
- 浏览器实测受阻：agent-browser 安装成功但 Chromium 下载超时（GCS 网络问题）；系统 ms-playwright 有 chromium-1234，但 bash 下 agent-browser open 反复 SIGTERM（沙箱限制），自动化验证未跑通
- 用户 11:10 提供真实系统截图：蓝色顶栏 + 白色双列侧栏 + 对象模型完整字段表格（含所属公司/适用范围/审计字段，字段映射配置列=数字+铅笔图标，操作=编辑/停用/删除）
- 按截图两轮修正最外层后，用户最终要求：去掉 header 和左右导航，只保留内容区（已执行，弹窗完整保留）
- 教训（再次确认）：改用户已有文件前必须先确认范围，禁止整文件重写；大改前先备份

## 对象复制映射扩展 — 第五轮讨论
- 继续讨论 `registry/discussions/对象复制映射扩展-设计讨论.md`
- 收口文档矛盾：#4 阻断复用已被 #15 自动复用替代；#7 特殊字段已随批次出范围消解；#8 批号对应从复制职责划出；#9 澄清为「要有物料侧入口、不对齐库存菜单权限」；对照表「唯一性校验阻断」改为自动复用
- 压力测试：#15「该入库物料直接引用」把复制和产物入库写混了。注册数据是主数据 1:1 字段，复用不改写则对象 B 不会出现在关联物料 tab；覆盖则会抢走对象 A 的锚点
- 建议 #18 用词修正：复制不产生入库单，不是库存挂接；空锚点可补写，已有锚点不改写
- 弹窗可改项落到 6.6 字段表，把 #14「整段延后」收成可评审清单
- 本轮待确认：
  - #18 复用不改写；空注册数据可补写
  - #19 对象→主数据弹窗：映射/注册数据/单位只读，编号可改，缺口手填
  - #20 主数据→对象弹窗：部门=当前用户，项目空，不写来源关联
  - #21 物料侧「复制到对象...」要做；支持批量逐条
  - #22 单位不映射，只搬数值，不做换算
  - #23 开启注册数据则映射保存必须已把该字段纳入主数据唯一校验
  - #24 反向：已有注册数据阻断；为空则新建对象并回写
  - #25 仅有效对象 / 启用主数据可作源；命中禁用主数据仍不新建
  - #26 开启注册数据时物料名称不作为有效映射行
  - #27 反向无对象侧组合判重，每次新建（第二次被 #24 挡住）
- 正式设计说明的批号表述、落库无条件写锚点等矛盾记在 6.8，确认后再改 V1.1
- 现有 prototype 只覆盖映射配置，复制确认弹窗需等 #19/#20 确认后再做

## 待办
- 等用户确认第 6 节建议 #18–#27（建议顺序见讨论文档 6.8）
- 确认后同步正式功能设计说明 V1.1，并补复制确认弹窗原型
- 浏览器自动化验证未跑通（沙箱 SIGTERM），后续可试 PowerShell 通道或用户实测
- 映射配置弹窗内页与真实系统的贴近度待用户确认
