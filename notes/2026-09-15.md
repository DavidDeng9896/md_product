# 2026-09-15

## 对象复制映射扩展 — 第三轮决策
- ①映射配置位置统一在对象上：对象映射关系设置并存两类映射——对象模型映射（对象↔对象）+ 对象与物料信息映射（对象↔物料主数据）；物料类型侧不设入口，1:1 绑定约束不变（决策 #1 更新）
- ②映射范围缩小：**仅基础信息+扩展信息，批次信息不映射**（决策 #3 替代原结论）。连带效果：父细胞/父质粒/父病毒/关联质粒/引物/特征基因/序列图谱/检测状态/来源实验/COA/供应商/货号等全部批次字段出范围，原"特殊字段专项讨论"大半消解
- ③主数据编号沿用现有规则：自动生成、新增可修改、不继承源编号（决策 #5 闭环）
- 扩展信息的字段能力与对象字段映射具体方案明确留待后续专项讨论
- 新增待讨论项 #8：列表字段（厂家/供应商，来源为通用配置）与对象下拉字段的选项值映射机制
- 剩余待讨论：#3 扩展字段专项、#4 双向弹窗形态、#5 判重复用交互、#6 主数据→对象遇工作流、#7 主数据初始状态+类型前置校验、#8 列表选项映射

## 第四轮决策（2026-09-15 下午）
- 新增参考文档：物料类型扩展字段配置 PRD.md（11 种扩展字段类型：复选框/单选框/下拉/单行文本/多行文本/计数器/滑块/开关/时间/图片/上传；选项类支持关联数据源-通用配置字典；通用配置含只读/key/校验/必填）
- ④双向弹窗形态：延后至原型设计阶段（决策 #14）
- ⑤列表/选项映射：同名选项自动匹配 + 不可匹配留空（决策 #11，兼容矩阵 5.5 节已细化两种选项来源）
- ⑥主数据→对象遇工作流：进"待审批"，与对象复制一致（决策 #12）
- ⑦主数据初始状态默认"启用"；物料类型需"已发布+启用"为复制前置（决策 #13）
- 文档 5.5 节新增：扩展字段 11 类型兼容矩阵 + 校验衔接 + 遗留细节
- 剩余待讨论仅 3 项：#5 判重阻断后复用交互、#9 只读扩展字段复制写入是否允许、#10 字典变更历史取值约束与复制兼容（偏实现层）

## 决策全部闭环（2026-09-15 16:29）
- #15 判重命中：**自动复用**——命中主数据唯一性校验时不新建、不阻断，该入库物料直接引用已有主数据，源对象不受影响（修正了此前"阻断并提示复用"的结论，决策 #4 的交互升级为系统自动挂接）
- #16 只读扩展字段：复制属**新增初始化**，只读是表单层约束，系统写入允许
- #17 字典变更：老数据不清空（沿用既有约束），复制新增的数据按最新字典选项文字匹配
- 至此 17 项决策全部闭环；剩余开放项仅：弹窗形态（原型阶段）、扩展字段校验规则集（实现层）
- 讨论文档已标注"决策已全部闭环，可进入正式功能设计说明编写"

## 原型制作（2026-09-15 17:20 起）
- 第 1 块交互（对象侧两种映射策略配置）的原型已产出：prototype/index.html
- 原型覆盖：对象模型列表入口（字段映射配置改为映射关系设置）、映射关系设置弹窗（两类映射并存、多物料类型、审计字段）、新增映射选择类型、字段映射配置弹窗（对象模型映射 vs 对象与物料信息映射切换、批次字段过滤、系统自动写入项提示、源字段下拉选择、Key 列展示）
- 原型反馈已确认：①入口叫"映射关系设置" ②列表加字段映射条数/审计字段 ③一个对象可配多个物料类型 ④映射对象显示改为"源对象：XXX → 当前对象" / "物料类型：XXX" ⑤源/目标列标题改为"源字段（目标对象/物料类型）" ⑥系统写入字段不展示 ⑦批次字段直接过滤不显示 ⑧源字段每行下拉 ⑨Key 列保留但次要
- 原型已补充真实数据：以 Antibody Complex → 实验产物（普通物料）为例，覆盖基础信息（物料名称/别名/规格型号/浓度/纯度/含量/生产厂家/级别）和扩展信息（储存条件/危险特性/是否低温保存/MSDS 文件/备注）；对象侧源字段包含名称/编号/别名/规格/浓度/纯度/含量/厂家/物料级别/储存条件等真实字段
- 系统写入字段在字段映射表中展示为蓝色提示"系统自动写入"（物料编号/注册数据/计量单位/计量小数位），明确告知用户无需配置
- 新增待讨论项：对象侧接入单位能力（两边都有/无单位时的映射处理）
- 待办项：扩展字段校验规则集、图片/上传字段大小数量上限

## 正式功能设计说明产出（2026-09-15 16:29）
- 新文件：对象复制映射扩展-功能设计说明.md（V1.0，PRD 格式，对齐《对象复制与工作流-功能设计说明.md》风格）
- 结构：映射配置设置（入口/两类映射并存/兼容矩阵）、复制功能（对象→主数据、主数据→对象）、溯源机制（注册数据锚点）、校验策略汇总、既有功能衔接、功能清单、遗留事项（弹窗原型/扩展字段校验规则/附件上限）
- 关键文件清单更新：该文档为评审/交付用主文档，讨论稿（对象复制映射扩展-设计讨论.md）保留决策过程

## 原型视觉迭代（2026-09-15 17:40–17:57）
- 接入 Remix Icon（CDN v4.5.0）；用户改动保留
- 字段映射配置表：行只读 + hover 显示"编辑"→ 行内编辑（保存/取消），已映射蓝色链接图标 / 未映射灰色占位
- 用户反馈：主表"操作"列不要 icon（纯文字链接）；弹窗细节粗糙需打磨
- 弹窗打磨：Modal1 卡片式表格+彩色映射类型标签；Modal2 必填红星+类型未选提示+按钮改"下一步"按类型联动（修复原确定按钮固定传 material 的 bug）；Modal3 返回按钮+灰底表单卡片+已映射 x/y 计数
- 全局：focus 蓝描边、按钮 hover 态、backdrop 微模糊
- 用户界面偏好：与真实系统对齐，操作列纯文字，Remix Icon 图标库

